// ─── Ship registry bootstrap ────────────────────────────────────────────────
// Import every ship config file here so they self-register on load.

export { aetherShipConfig } from './aether';

// Future ships:
// export { voidRunnerShipConfig } from './voidrunner';
